node_modules/
dist/
.env
.env.local
.env.*.local
*.log
.DS_Store
.idea/
.vscode/
*.swp
*.swo
*~
.bun/
bun.lockb

# Phase 2 voice: downloaded binaries/models (large, fetched by scripts/setup-voice.sh)
tools/piper/
tools/whisper/
models/piper/
models/whisper/
tools/chatterbox/

# [ADDED 2026-09-01] Chatterbox voice-clone reference clip - Gavin's own
# real recorded voice sample. Personal audio data, not source code; never
# belongs in git history.
assets/voice/

# Python bytecode cache (scripts/ has real .py files: mic_capture.py,
# wakeword_detect.py, whisper_transcribe.py)
__pycache__/
*.pyc

# [ADDED 2026-09-02] Native JARVIS HUD (native-hud/) - real C#/WPF source
# is tracked, but its build output isn't, same pattern as tools/piper etc.
# above: built locally via setup-native-hud.ps1, not committed.
native-hud/bin/
native-hud/obj/

# [ADDED 2026-09-02] Real runtime state from start-jarvis.ps1/stop-jarvis.ps1
# (background/auto-run mode) - a live PID file and a graceful-shutdown flag
# file cli.ts's `listen` command polls for (see its own comment). Neither
# is source code and both are meaningless outside a live run.
.jarvis.pid
.jarvis-stop
logs/
