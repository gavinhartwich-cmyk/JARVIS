{
  "name": "jarvis",
  "version": "0.1.0",
  "type": "module",
  "description": "JARVIS: Personal AI Operating System",
  "main": "dist/cli.js",
  "scripts": {
    "dev": "bun run src/cli.ts",
    "build": "bun build src/cli.ts --outdir dist",
    "test": "bun test",
    "test:live": "bun test src/**/*.test.ts",
    "db:push": "bun run src/db/migrations/push.ts",
    "db:reset": "bun run src/db/migrations/reset.ts"
  },
  "dependencies": {
    "drizzle-orm": "^0.30.0",
    "drizzle-kit": "^0.30.0",
    "pg": "^8.11.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/pg": "^8.11.0"
  }
}
